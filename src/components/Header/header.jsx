import React from 'react';

import './header.css';

/*
    Header Page: Component
*/

export const Header = props => (
    <header className="headText">
        <h1 >Form Builder</h1>
        <p>Build your form now!</p>
    </header>
);